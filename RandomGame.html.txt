<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>랜덤 뽑기 게임 🎲</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      text-align: center;
      background: linear-gradient(135deg, #ffe6e6, #e6f7ff, #e6ffe6);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      margin: 0;
    }
    h1 {
      font-size: 36px;
      color: #444;
      margin-bottom: 20px;
    }
    textarea {
      width: 300px;
      height: 120px;
      padding: 10px;
      border: 2px solid #ccc;
      border-radius: 12px;
      font-size: 16px;
      resize: none;
      outline: none;
      background: #fff;
      box-shadow: 2px 2px 8px rgba(0,0,0,0.1);
    }
    button {
      margin-top: 20px;
      padding: 12px 25px;
      font-size: 18px;
      font-weight: bold;
      color: white;
      background: linear-gradient(135deg, #ff99cc, #66ccff);
      border: none;
      border-radius: 25px;
      cursor: pointer;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }
    button:hover {
      transform: scale(1.05);
      box-shadow: 0px 4px 12px rgba(0,0,0,0.2);
    }
    .result {
      margin-top: 40px;
      font-size: 28px;
      font-weight: bold;
      color: #333;
      opacity: 0;
      transform: scale(0.8);
      transition: opacity 0.5s ease, transform 0.5s ease;
    }
    .result.show {
      opacity: 1;
      transform: scale(1.1);
      animation: pop 0.5s forwards;
    }
    @keyframes pop {
      0% { transform: scale(0.8); }
      50% { transform: scale(1.2); }
      100% { transform: scale(1); }
    }
  </style>
</head>
<body>
  <h1>랜덤 뽑기 게임 🎲</h1>
  <p>아래에 항목들을 줄바꿈(Enter)으로 구분해서 입력하세요:</p>
  <textarea id="itemList" placeholder="예: 아이스크림&#10;피자&#10;햄버거"></textarea><br>
  <button onclick="pickRandom()">✨ 추첨하기 ✨</button>

  <div class="result" id="result"></div>

  <script>
    function pickRandom() {
      const text = document.getElementById("itemList").value.trim();
      const resultBox = document.getElementById("result");
      if (!text) {
        alert("항목을 입력하세요!");
        return;
      }
      const items = text.split(/\n/).map(item => item.trim()).filter(Boolean);
      const randomIndex = Math.floor(Math.random() * items.length);
      const chosen = items[randomIndex];
      
      resultBox.textContent = "🎉 결과: " + chosen + " 🎉";
      resultBox.classList.remove("show");
      setTimeout(() => resultBox.classList.add("show"), 50);
    }
  </script>
</body>
</html>
